'use client';
import { GoogleLogin } from '@react-oauth/google';import Link from 'next/link';import { useRouter,useSearchParams } from 'next/navigation';import { useState } from 'react';import { toast } from 'react-toastify';import { api } from '@/libs/api.js';import { useAuth } from '@/libs/auth-context.js';
export default function AuthForm({mode}){const register=mode==='register';const [form,setForm]=useState({name:'',email:'',photoURL:'',password:''});const {setUser}=useAuth();const router=useRouter();const params=useSearchParams();const submit=async e=>{e.preventDefault();try{const data=await api(`/auth/${register?'register':'login'}`,{method:'POST',body:JSON.stringify(form)});if(register){toast.success('Registration successful. Please log in.');router.push('/login')}else{setUser(data.user);toast.success('Login successful');router.push(params.get('next')||'/dashboard')}}catch(error){toast.error(error.message)}};const google=async credential=>{try{const data=await api('/auth/google',{method:'POST',body:JSON.stringify({credential})});setUser(data.user);router.push('/dashboard')}catch(e){toast.error(e.message)}};return <section className="px-4 py-16"><div className="mx-auto grid max-w-5xl gap-8 lg:grid-cols-2 lg:items-center"><div><p className="section-label">JWT authentication route</p><h1 className="mt-3 font-display text-5xl font-black">{register?'Create your PromptGrid account':'Log in to your PromptGrid account'}</h1><p className="mt-5 leading-7 muted">Secure sessions use HTTP-only JWT cookies. New accounts receive the User role and Free plan.</p></div><form onSubmit={submit} className="hard-card grid gap-4 rounded-[2rem] p-6 sm:p-8"><h2 className="font-display text-3xl font-black">{register?'Create account':'Welcome back'}</h2>{register?<><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="input-box rounded-2xl px-4 py-4" placeholder="Name"/><input value={form.photoURL} onChange={e=>setForm({...form,photoURL:e.target.value})} className="input-box rounded-2xl px-4 py-4" placeholder="Photo URL"/></>:null}<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="input-box rounded-2xl px-4 py-4" placeholder="Email address"/><input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="input-box rounded-2xl px-4 py-4" placeholder="Password"/><button className="btn-lime rounded-2xl px-5 py-4 font-black">{register?'Register':'Log in'}</button>{process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID?<div className="flex justify-center"><GoogleLogin onSuccess={({credential})=>google(credential)} onError={()=>toast.error('Google login failed')}/></div>:null}<p className="text-center muted">{register?'Already have an account?':'Not registered?'} <Link className="font-black underline" href={register?'/login':'/register'}>{register?'Login':'Create an account'}</Link></p></form></div></section>}
